sudo: false
os:
  - linux
  - osx
env:
  - TOOLSET=gcc
  - TOOLSET=darwin
  - TOOLSET=clang
matrix:
  exclude:
    - os: linux
      env: TOOLSET=darwin
    - os: osx
      env: TOOLSET=gcc
script:
  - ./bootstrap.sh --with-toolset=${TOOLSET}
